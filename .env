# Port the Express + WebSocket server listens on
PORT=4000

# MongoDB connection string — change host/db name as needed
MONGO_URI=mongodb://127.0.0.1:27017/chat

# Origin of the Next.js frontend (used for CORS)
CLIENT_ORIGIN=http://localhost:3000
